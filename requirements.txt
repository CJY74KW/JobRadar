# 채용 정보 웹 애플리케이션 — 필요 라이브러리
# 설치 명령: pip install -r requirements.txt

# 웹 서버 (백엔드)
flask>=3.0.0
gunicorn>=21.2.0

# API 호출
requests>=2.31.0

# 데이터 처리
pandas>=2.0.0

# HTML 파싱 (확장용)
beautifulsoup4>=4.12.0
lxml>=4.9.0

# Excel 저장
openpyxl>=3.1.0

# 환경변수 관리
python-dotenv>=1.0.0
